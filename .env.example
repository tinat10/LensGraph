# ─── Database (Neon — recommended) ───────────────────────────────────────────
#
# 1. Sign up free at https://neon.tech
# 2. Create a project (name it "lensgraph")
# 3. Open Dashboard → Connect → copy the connection string
# 4. Paste it below (keep sslmode=require if Neon includes it)
#
# Example:
# DATABASE_URL="postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require"
#
DATABASE_URL="postgresql://USER:PASSWORD@HOST/neondb?sslmode=require"

# ─── Auth (NextAuth / Auth.js) ───────────────────────────────────────────────
# Generate with: openssl rand -base64 32
AUTH_SECRET="your-auth-secret-here"
AUTH_URL="http://localhost:3000"

# GitHub OAuth — create at https://github.com/settings/developers
AUTH_GITHUB_ID="your-github-oauth-client-id"
AUTH_GITHUB_SECRET="your-github-oauth-client-secret"

# ─── Cloudinary ──────────────────────────────────────────────────────────────
CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_API_KEY="your-api-key"
CLOUDINARY_API_SECRET="your-api-secret"

# ─── Future integrations (not required for MVP) ──────────────────────────────
# TODO(OpenAI Vision): AI captions, tags, mood/subject classification
# OPENAI_API_KEY="sk-..."

# TODO(Mapbox): Reverse geocoding GPS coordinates to place names
# MAPBOX_ACCESS_TOKEN="pk...."

# TODO(OpenWeather): Weather context on published story pages
# OPENWEATHER_API_KEY="your-openweather-key"
